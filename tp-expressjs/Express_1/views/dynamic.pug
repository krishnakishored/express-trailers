html
    head
        title=name
    body
        h1=name
        h3 Greeting from #{name}
        a(href=url) URL


// To put text inside of a tag, we have 3 methods 
// This method of using values is called interpolation